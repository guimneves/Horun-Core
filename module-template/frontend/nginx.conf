server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: qualquer rota que não seja um arquivo real cai no index.html,
    # deixando o router do frontend decidir a página no cliente.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
